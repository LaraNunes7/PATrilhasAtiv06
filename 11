using System;

class Program
{
    static void Main()
    {
        // Cria um vetor de inteiros
        int[] vetor = new int[] { 10, 20, 30, 40, 50 };

        // Imprime os valores do vetor
        for (int i = 0; i < vetor.Length; i++)
        {
            Console.WriteLine("Valor do elemento " + i + ": " + vetor[i]);
        }
    }
}
